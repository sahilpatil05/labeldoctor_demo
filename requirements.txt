# Label Doctor - Demo Version
# Core dependencies
Flask==2.3.3
Flask-CORS==4.0.0

# Optional: OCR support (app works without these)
# If installation fails, the app will gracefully fall back to manual entry
Pillow==10.0.0
pytesseract==0.3.10